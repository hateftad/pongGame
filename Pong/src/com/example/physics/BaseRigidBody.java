package com.example.physics;
/*
import org.jbox2d.dynamics.Body;
import org.jbox2d.dynamics.BodyDef;
import org.jbox2d.dynamics.BodyType;
import org.jbox2d.dynamics.World;

import com.example.math.Vector2;
import com.example.utils.Converters;

public class BaseRigidBody {

	protected Body m_body;
	protected Vector2 m_worldPosition;
	
	public BaseRigidBody(World world, Vector2 pos, BodyType type)
	{
		m_worldPosition = new Vector2();
		
	}
	
	public Body createBody(World world, Vector2 pos, float angle, BodyType type)
	{
		BodyDef def = new BodyDef();
		def.type = type;
		def.position.set(Converters.Vector2ToVec2(pos));
		def.angle = 0.0f;
		return world.createBody(def);
	}
	
}
*/